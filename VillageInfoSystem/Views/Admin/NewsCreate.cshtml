@model VillageInfoSystem.Models.News
@{
    ViewData["Title"] = "নতুন সংবাদ যোগ";
    Layout = "_AdminLayout";
}

<div class="breadcrumb"><a href="/Admin/NewsList">সংবাদ</a> › নতুন সংবাদ</div>

<div class="card" style="max-width:750px">
    <div class="card-title" style="margin-bottom:1.2rem">📰 নতুন সংবাদ যোগ করুন</div>
    <form asp-action="NewsCreate" method="post">
        @Html.AntiForgeryToken()
        <div class="form-group">
            <label asp-for="Title"></label>
            <input asp-for="Title" class="form-control" placeholder="সংবাদের শিরোনাম লিখুন" />
            <span asp-validation-for="Title" style="color:red;font-size:0.82rem"></span>
        </div>
        <div class="form-group">
            <label asp-for="Description"></label>
            <textarea asp-for="Description" class="form-control" placeholder="সংবাদের বিস্তারিত বিবরণ"></textarea>
        </div>
        <div class="form-grid">
            <div class="form-group">
                <label asp-for="Category"></label>
                <select asp-for="Category" class="form-control">
                    <option>সাধারণ</option>
                    <option>শিক্ষা</option>
                    <option>উন্নয়ন</option>
                    <option>কৃষি</option>
                    <option>স্বাস্থ্য</option>
                    <option>ধর্মীয়</option>
                    <option>খেলাধুলা</option>
                    <option>সংস্কৃতি</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Icon"></label>
                <input asp-for="Icon" class="form-control" placeholder="যেমন: 📰 🏫 🛣️" />
            </div>
        </div>
        <div class="form-grid">
            <div class="form-group">
                <label asp-for="PublishedAt"></label>
                <input asp-for="PublishedAt" type="datetime-local" class="form-control" />
            </div>
            <div class="form-group" style="display:flex;align-items:center;gap:2rem;padding-top:1.8rem">
                <div class="form-check">
                    <input asp-for="IsFeatured" type="checkbox" />
                    <label asp-for="IsFeatured" style="font-weight:500">শীর্ষ সংবাদ</label>
                </div>
                <div class="form-check">
                    <input asp-for="IsActive" type="checkbox" />
                    <label asp-for="IsActive" style="font-weight:500">সক্রিয়</label>
                </div>
            </div>
        </div>
        <div style="display:flex;gap:1rem;margin-top:0.5rem">
            <button type="submit" class="btn btn-primary">💾 সংরক্ষণ করুন</button>
            <a href="/Admin/NewsList" class="btn btn-outline">বাতিল</a>
        </div>
    </form>
</div>
